apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: actions-runner-controller
  namespace: runners
spec:
  interval: 10m
  chart:
    spec:
      chart: actions-runner-controller
      version: ">=0.23.0"
      sourceRef:
        kind: HelmRepository
        name: actions-runner-controller
        namespace: runners
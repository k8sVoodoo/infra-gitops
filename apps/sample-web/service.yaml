apiVersion: v1
kind: Service
metadata:
  name: sample-web
  namespace: sample-web
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: sample-web
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080